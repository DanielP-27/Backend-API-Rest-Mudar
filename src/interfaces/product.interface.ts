// Interface: unicamente se modela y define la estructura de datos y sus tipos, no se agregan funcionalidades o metodos //
export interface product {
    name: string;
    price: number;
    description: string;
}